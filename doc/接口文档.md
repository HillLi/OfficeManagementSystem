# 高校办公管理系统接口文档

> 2026-05-26 更新：用印申请改为草稿后上传真实材料再提交审批，并新增材料维护接口；请结合《接口补充-流程闭环.md》作为当前完整接口说明。

接口统一返回：

```json
{
  "success": true,
  "message": "ok",
  "data": {}
}
```

## 登录与用户

| 方法 | 地址 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录 |
| GET | `/api/auth/users` | 查询完整用户列表，仅管理员 |
| GET | `/api/auth/user-options` | 查询姓名与部门选项，供已登录用户办理表单使用 |

登录请求：

```json
{
  "username": "user",
  "password": "123456"
}
```

## 字典中心

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/dictionaries` | 查询版本化字典目录，包含停用历史项 |
| GET | `/api/dictionaries/version` | 查询字典目录版本 |
| GET | `/api/admin/dictionaries/types` | 管理员查询字典分类 |
| POST | `/api/admin/dictionaries/types` | 管理员新增字典分类 |
| PUT | `/api/admin/dictionaries/types/{dictType}` | 管理员更新字典分类 |
| GET | `/api/admin/dictionaries/types/{dictType}/items` | 管理员查询字典项目 |
| POST | `/api/admin/dictionaries/types/{dictType}/items` | 管理员新增字典项目 |
| PUT | `/api/admin/dictionaries/types/{dictType}/items/{code}` | 管理员更新或停用字典项目 |

公文文种/密级、用印事项等级、会议类型与场地、报告类型/密级、差旅枚举字段和附件密级均只接受处于启用状态的字典项。

## 公文管理

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/documents` | 查询公文列表 |
| POST | `/api/documents` | 新建公文草稿 |
| POST | `/api/documents/{id}/submit` | 提交审批 |
| POST | `/api/documents/{id}/ai-review` | AI智能审核 |
| POST | `/api/documents/ai-draft` | AI起草 |

新建公文请求：

```json
{
  "title": "关于开展办公管理系统试运行的通知",
  "docType": "通知",
  "urgency": "普通",
  "secrecyLevel": "公开",
  "knowledgeScope": "全校",
  "content": "各单位：为提高学校办公流程办理效率，现组织开展系统试运行工作。",
  "applicantId": 2
}
```

## 印章管理

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/seals` | 查询印章台账 |
| GET | `/api/seals/applications` | 查询用印申请 |
| POST | `/api/seals/applications` | 保存用印申请草稿，响应包含 `sealName` 与 `materialCount` |
| POST | `/api/seals/applications/{id}/submit` | 草稿已具备至少一份有效材料后提交审批 |
| POST | `/api/seals/applications/{id}/used?keeperId=8` | 用印登记 |
| POST | `/api/seals/applications/{id}/returned?keeperId=8` | 归还确认 |

用印申请请求：

```json
{
  "sealId": 1,
  "applicantId": 2,
  "purpose": "系统试运行通知材料用印",
  "copies": 2,
  "takeOut": false,
  "matterLevel": "常规事项"
}
```

保存草稿后，调用 `/api/workflow/attachments/upload` 上传真实材料文件；不得通过 `materialUrl` 跳过材料校验。

## 会议管理

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/meetings/rooms` | 查询会议室 |
| POST | `/api/meetings/recommend` | 会议室智能推荐 |
| GET | `/api/meetings` | 查询会议 |
| POST | `/api/meetings` | 提交会议申请 |

会议申请请求：

```json
{
  "title": "系统试运行培训会",
  "roomId": 1,
  "organizerId": 2,
  "startTime": "2026-05-22T09:00:00",
  "endTime": "2026-05-22T11:00:00",
  "expectedCount": 60,
  "venueType": "室内",
  "meetingType": "国内管理会议",
  "budget": 3000
}
```

## 差旅审批

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/travels` | 查询差旅申请 |
| POST | `/api/travels` | 提交差旅申请并自动校验费用 |

差旅申请请求：

```json
{
  "applicantId": 2,
  "destination": "上海",
  "startDate": "2026-06-01",
  "endDate": "2026-06-03",
  "reason": "参加高校信息化建设会议",
  "staffLevel": "三类",
  "travelType": "教学科研业务",
  "transport": "高铁二等座",
  "budget": 2600
}
```

## 请示报告

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/reports` | 查询请示报告 |
| POST | `/api/reports` | 提交请示报告 |

## 通用审批

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/approvals?bizType=document&bizId=1001` | 查询审批历史 |
| POST | `/api/approvals/{bizType}/{bizId}` | 审批通过或退回 |
| GET | `/api/workflow/guide?bizType=document&bizId=1001` | 查询流程导览图，包含完整路径、当前节点、角色、处理人、意见与时间 |

审批请求：

```json
{
  "operatorId": 3,
  "action": "approve",
  "opinion": "同意"
}
```

## 统计报表

| 方法 | 地址 | 说明 |
|------|------|------|
| GET | `/api/dashboard` | 查询工作台统计指标 |
