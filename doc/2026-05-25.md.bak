# 高校办公管理系统需求与设计符合性测试报告

**报告日期**：2026-05-25  
**核查范围**：`doc/需求.md`、`doc/设计.md`、后端 `backend/`、前端 `frontend/`、MySQL 初始化与运行链路  
**结论**：当前代码具备基础登录、流程状态、附件/审计/提醒的数据结构和部分业务页面，但**尚不符合“完整高校办公管理系统”验收要求**。本次检查发现多项可实际复现的越权与制度落地缺口，且真实 MySQL 配置当前无法启动后端。

## 1. 关键缺陷清单

| 编号 | 等级 | 问题 | 需求/设计依据 | 代码与运行证据 | 影响 |
| --- | --- | --- | --- | --- | --- |
| F-01 | 严重 | 秘密附件和审批历史可被无关财务人员读取 | 需求要求密级、知悉范围、涉密访问控制与访问日志；设计 `secrecy_level`、`knowledge_scope`、`secret_until` 及访问记录 | `WorkflowService.java:29-50` 与 `ApprovalService.java:49-54` 无业务权限过滤。接口实测：财务人员读取秘密公文附件数量 `1`、审批历史数量 `2` | 涉密内容泄露，无法满足保密制度与最小知悉范围 |
| F-02 | 严重 | 公文提交和归档缺少操作权限校验 | 权限矩阵要求发起人提交、党办校办归档；流程需可追责 | `DocumentService.java:74-94` 只校验状态。接口实测：财务人员将他人秘密公文提交到 `pending_dept`，并将审批后的公文归档为 `archived` | 流程可被无关角色篡改，审计责任失真 |
| F-03 | 严重 | MySQL 运行配置无法启动系统 | 需求要求 MySQL 落库并提供初始化脚本 | `MysqlDataLoader.java:228-283` 启动即查询新增流程表；真实启动失败：`Table 'office_management_system.sys_attachment' doesn't exist`。`schema.sql:151-227` 虽定义新表，但当前库未同步 | 生产型持久化运行路径不可用，只能依赖内存模式 |
| F-04 | 高 | 会议纪要、请示批复、差旅报销均可由无关角色完成 | 需求要求部门、财务、领导和经办岗位分工，业务闭环应受权限控制 | `MeetingService.java:119-134`、`ReportService.java:70-84`、`TravelService.java:92-114` 未校验操作角色/归属。接口实测：财务人员成功归档会议纪要、批复内部报告、无票据发起他人差旅报销 | 核心闭环虽有状态变化，但不具备合规业务权限 |
| F-05 | 高 | 大型活动缺少提前 15 个工作日校验 | 设计映射表要求室内大于 500 人/室外大于 100 人且至少提前 15 个工作日申请 | `MeetingService.java:88-115,163-166` 仅判断人数与三类材料。实测 2026-05-25 申请 2026-05-26 的 501 人室内活动，成功进入 `pending_security` | 不符合大型活动安全许可时限规则 |
| F-06 | 高 | 印章事项等级未驱动分级审批，移交闭环缺失 | 设计要求按事项等级动态审批，并建设 `oa_seal_transfer` | `SealApplyRequest` 存储 `matterLevel`，但 `ApprovalFlowConfig.java:19-20` 仅区分校级/部门流程；实测部门“常规事项”和“重大事项”均为 `pending_dept`。实际 schema 未实现设计中的移交表 | 用印审批规则与制度映射不一致，印章移交不可追踪 |
| F-07 | 高 | 前端审批和用印闭环无法由用户在界面完成 | 需求要求审批、退回、登记、归还等可操作闭环 | `frontend/src/api.js:44` 有审批 API，但 `frontend/src/views/Approvals.vue` 仅展示任务/通知/记录，无同意或退回按钮；`Seals.vue` 仅提交申请，无已用/归还操作调用 | 后端部分接口存在，但前端不能完成完整业务工作流 |
| F-08 | 中 | 保密与 AI 规则落地不足 | 需求要求内部敏感/涉密材料不得直接交外部 AI；设计要求密级判断与审计 | `DocumentService.java:106-107` 直接调用起草服务；保密拦截仅覆盖秘密/机密/绝密，不覆盖“内部”资料脱敏和 AI 调用审计；数据库也无 `secret_until`、访问日志实体 | 无法证明内部材料和 AI 使用合规 |
| F-09 | 中 | 设计技术栈与实际实现不一致 | 设计文档承诺 Spring Boot 3.x、MyBatis-Plus、Sa-Token、BCrypt | `backend/pom.xml:9-41` 实际为 Spring Boot `2.5.6` + `spring-jdbc`；`PasswordService.java:51` 实际采用 PBKDF2 | 文档不能作为当前实现的真实设计基线，验收说明失真 |

## 2. 需求覆盖核对

| 模块 | 已实现内容 | 与需求/设计不匹配或缺失内容 | 判定 |
| --- | --- | --- | --- |
| 用户与认证 | 登录、角色种子、PBKDF2 密码、部分管理接口拦截 | Token 无过期/服务端注销；普通业务接口缺少细粒度授权；设计技术方案不一致 | 部分符合 |
| 公文管理 | 创建、提交、三级审批、归档、附件、AI 审查入口 | 无分发/签收/催办/转办/版本管理；提交归档越权；内部 AI 合规控制不足 | 不符合完整闭环 |
| 印章管理 | 用印申请、材料地址、份数、外带标志、使用/归还后端接口 | 事项等级未驱动审批；无遗失/停用/移交；前端无使用/归还入口 | 不符合 |
| 会议与活动 | 场地容量、冲突判断、大型活动阈值、三类安全材料、纪要字段 | 无 15 工作日校验；纪要可越权归档；会议费只校验单一预算额，未分住宿/伙食/其他维度 | 不符合 |
| 请示与保密 | 请示提交、保密审查状态、批复字段 | 无知悉范围/保密期限/解密/借阅访问日志；批复越权 | 不符合 |
| 差旅与报销 | 差旅提交、交通类别校验、金额计算、财务审批状态 | 标准主要硬编码而非配置驱动；无经费来源、超标说明审批；无票据且非本人可报销；无归档端点 | 不符合 |
| 待办/提醒/审计 | 流程实例、待办、通知、操作日志基础模型 | 附件与审批历史越权；审计日志未覆盖访问控制和 AI 调用；前端不能办理待办 | 部分符合 |
| 统计报表 | 首页部分汇总数据 | 缺少设计所列专项报表、数据范围权限与导出 | 不符合 |

## 3. 制度链接与规则实现核验

### 3.1 核心来源链接验证

本次于 2026-05-25 对业务实现直接依赖的官方来源进行了访问和内容核对：

| 制度来源 | 链接状态 | 可核对的关键规则 | 当前实现匹配情况 |
| --- | --- | --- | --- |
| [北京大学印章管理规定](https://xxgk.pku.edu.cn/gksx/jbxx/gzzd/1065xbxxgk144899.htm) | 可访问 | 用印登记、移交及审批要求 | 有申请和登记片段，事项分级与移交未完成 |
| [北京大学保密工作规定](https://xxgk.pku.edu.cn/gksx/jbxx/gzzd/1065xbxxgk144898.htm) | 可访问 | 密级、知悉范围、载体管理 | 只有部分密级字段，读取权限与访问日志未落实 |
| [北京大学大型活动安全管理规定](https://xxgk.pku.edu.cn/gksx/jbxx/gzzd/1065xbxxgk144903.htm) | 可访问 | 室内大于 500 人、室外大于 100 人；提前 15 个工作日；安全材料 | 阈值与三类材料部分实现，提前期未实现 |
| [北京大学会议费管理暂行办法](https://xxgk.pku.edu.cn/docs/20241129103911566485.pdf) | PDF 可访问 | 会议类型与综合定额、住宿/伙食/其他费用范围 | 仅实现会议类型到单一预算上限的简单比较 |
| [北京大学国内差旅费管理暂行办法](https://xxgk.pku.edu.cn/docs/20241129103948804688.pdf) | PDF 可访问 | 出行等级、住宿/伙食/交通补助及报销凭证要求 | 有基础测算，未完整覆盖报销规则与配置化标准 |

### 3.2 链接与设计文档本身的问题

1. `doc/设计.md:11` 声称“所有规章制度均来自北京大学官方信息公开网”，但 `doc/设计.md:46` 引用的是国务院网站的《中华人民共和国政府信息公开条例》，该表述与链接清单自身矛盾。
2. `doc/设计.md:46` 的 `http://www.gov.cn/...` 链接在本次自动访问中不可用；相应 HTTPS 地址在测试环境返回 `403 Forbidden`，不能继续维持“已验证可访问”的无条件结论。
3. 设计映射中写明的大型活动提前期、印章事项分级、保密访问日志、差旅配置化标准，均尚未与当前代码流程一一对应。

## 4. 深度运行测试记录

### 4.1 自动化构建与已有测试

| 测试项 | 执行方式 | 结果 | 说明 |
| --- | --- | --- | --- |
| 后端测试 | `D:\dev\maven\apache-maven-3.9.14\bin\mvn.cmd test`，目录 `backend/` | PASS | `Tests run: 35, Failures: 0, Errors: 0, Skipped: 0` |
| 前端构建 | `npm run build`，目录 `frontend/` | PASS | Vite `v6.4.2` 成功构建；存在第三方依赖注释处理 warning，不阻断构建 |
| MySQL 连通与结构读取 | MySQL `8.0.39`，只读查询 | PARTIAL PASS | `office_management_system` 已存在，`root/123456` 可读取；当前表结构未同步完整流程表 |
| MySQL profile 启动 | `spring-boot:run` + `mysql` profile，端口 `18081` | FAIL | 启动失败于缺少 `sys_attachment`；未执行写入型初始化脚本，避免修改现有数据库 |

### 4.2 权限与制度反例测试

测试使用端口 `18080` 的内存仓储实例，不修改 MySQL 业务数据。所有临时服务测试后已关闭。

| 场景 | 期望 | 实际结果 | 判定 |
| --- | --- | --- | --- |
| 财务人员提交普通办公人员创建的秘密公文 | 拒绝 | 返回成功，状态为 `pending_dept` | FAIL |
| 财务人员读取该秘密公文附件 | 拒绝/按知悉范围过滤并记录访问 | 返回附件 `1` 条 | FAIL |
| 财务人员读取该公文审批历史 | 拒绝/按权限过滤 | 返回审批记录 `2` 条 | FAIL |
| 财务人员归档已审批公文 | 仅党办校办可归档 | 返回成功，状态为 `archived` | FAIL |
| 次日举办 501 人室内活动申请 | 因不足 15 个工作日而拒绝 | 材料齐全后返回成功，状态为 `pending_security` | FAIL |
| 部门印章常规事项与重大事项审批状态 | 重大事项触发不同级别审批 | 两者均为 `pending_dept` | FAIL |
| 财务人员无票据代替发起人提交差旅报销 | 拒绝 | 返回成功，状态为 `pending_finance`，金额 `880` | FAIL |
| 财务人员归档已审批会议纪要 | 拒绝 | 返回成功，状态为 `archived` | FAIL |
| 财务人员批复并归档已审批内部报告 | 拒绝 | 返回成功，状态为 `archived` | FAIL |

### 4.3 前端运行验证

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 前端开发服务可启动 | PASS | `http://127.0.0.1:5173/` 返回 `200` |
| 真实浏览器交互自动化与截图 | BLOCKED | 内置浏览器连接资源缺失；备用 Playwright CLI 需临时获取并执行第三方包，安全策略未允许本次下载执行 |
| 界面闭环源码核对 | FAIL | `Approvals.vue` 无审批动作按钮；`api.js` 虽定义 `approve` 未在该页面调用；`Seals.vue` 无使用/归还操作入口 |

## 5. MySQL 初始化与迁移核对

1. 默认配置 `application.properties` 使用 `oms.repository=memory`，真实持久化必须主动切换 `mysql` profile。
2. `application-mysql.properties` 的数据库密码为 `123456`，而 `backend/scripts/init-mysql.ps1` 的默认参数密码为空；在当前机器上使用脚本默认值会连接失败，初始化命令必须显式传入密码或修正文档/脚本默认值。
3. 当前数据库只包含早期核心表，未包含 `schema.sql` 已定义的 `sys_attachment`、`sys_operation_log`、`sys_notification`、`oa_flow_instance`、`oa_flow_task` 等表，导致真实启动失败。
4. 本次测试未自动执行初始化脚本，因为它会修改现有 MySQL 库；应在备份或确认可覆盖的测试库上完成初始化/迁移后，重新执行 MySQL 全链路回归测试。

## 6. 修复优先级建议

| 优先级 | 应处理事项 | 验收回归重点 |
| --- | --- | --- |
| P0 | 为附件、审批记录、公文提交/归档、会议纪要、报告批复、差旅报销增加业务级授权与保密访问控制 | 跨角色反例必须返回 `403`；涉密访问有日志和知悉范围过滤 |
| P0 | 完成数据库迁移/初始化的一致启动链路，并统一脚本凭据说明 | 全新库初始化成功；`mysql` profile 启动、登录、完整流程持久化成功 |
| P1 | 实现大型活动提前 15 个工作日校验、印章事项分级/移交、差旅凭证与超标审批规则 | 对照制度条款编写自动化正反例 |
| P1 | 补齐前端审批、退回、用印登记/归还等操作入口 | 五类业务从页面可完整办理并显示流程/通知/审计 |
| P2 | 修订设计文档技术栈、来源链接可访问性声明及未实现表结构说明 | 文档描述与实际依赖、密码方案、规则流程完全一致 |

## 7. 最终判定

当前系统可以作为继续开发和修复的基础，但不能判定为需求文档与设计文档所要求的完整高校办公管理系统。已有 `35` 项后端测试和前端构建通过，覆盖的是基础功能与部分正向流程；本报告新增的运行反例证明，权限隔离、保密合规、制度规则执行、MySQL 可部署性和前端办理闭环仍存在验收阻断问题。
