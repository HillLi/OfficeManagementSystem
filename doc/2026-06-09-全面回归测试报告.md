# 2026-06-09 全面回归测试报告

## 1. 测试结论

本次回归按 `doc/回归测试标准.md` 执行，重点覆盖登录、角色权限、页面入口、按钮/弹窗、后端业务规则、前端构建和多角色 API 烟测。

结论：通过。

- 未发现阻断级 P0/P1 缺陷。
- 后端全量测试通过：132 个测试，0 失败，0 错误。
- 前端全量测试通过：28 个测试文件，106 个测试，0 失败。
- 前端生产构建通过。
- 多角色 API 烟测通过，覆盖 8 个演示账号。
- 本次新增 5 个前端页面级测试文件，补齐此前没有独立页面测试的页面。

## 2. 测试环境

| 项目 | 结果 |
|---|---|
| 日期 | 2026-06-09 |
| 工作区 | `D:\work\xianyu\OfficeManagementSystem` |
| 后端 | Spring Boot 2.5.6, Java 8 |
| 前端 | Vue 3, Vite, Vitest, Element Plus |
| Node | v24.11.1 |
| npm | 11.6.2 |
| Java | 1.8.0_421 |
| Maven | `D:\dev\maven\apache-maven-3.9.14\bin\mvn.cmd` |
| 本地服务 | `http://localhost:8080`, `http://localhost:5173` |

## 3. 自动化验证命令

### 3.1 后端全量测试

```powershell
cd backend
D:\dev\maven\apache-maven-3.9.14\bin\mvn.cmd test
```

结果：

- Tests run: 132
- Failures: 0
- Errors: 0
- Skipped: 0
- BUILD SUCCESS

覆盖重点：

- 登录与认证 token
- 权限回归
- 公文闭环与 AI 安全
- 用印材料管理
- 会议参与人和纪要
- 邮件集成
- 公告集成
- 字典中心
- 工作流导览
- 统计与工作台数据范围
- 设计模式相关测试
- MySQL Schema 契约测试

### 3.2 前端全量测试

```powershell
cd frontend
npm run test
```

结果：

- Test Files: 28 passed
- Tests: 106 passed
- Failures: 0

本次前端测试数从 95 个增加到 106 个。

### 3.3 前端生产构建

```powershell
cd frontend
npm run build
```

结果：

- 2253 modules transformed
- built in 11.53s
- exit code 0

备注：构建期间 Rollup 对 `node_modules/@vueuse/core/dist/index.js` 的 `/* #__PURE__ */` 注释位置给出警告，会移除该注释；这是第三方依赖构建警告，不影响本次构建结果。

### 3.4 Diff 格式检查

```powershell
git diff --check
```

结果：通过，无空白或补丁格式问题。

## 4. 多角色 API 烟测

执行范围：

- `POST /api/auth/login`
- `GET /api/dashboard`
- `GET /api/announcements/latest`
- `GET /api/admin/users`

账号覆盖：

| 账号 | roleKey | 登录 | 工作台 | 最新公告 |
|---|---|---|---|---|
| admin | admin | 通过 | 通过 | 通过 |
| user | office_user | 通过 | 通过 | 通过 |
| head | dept_head | 通过 | 通过 | 通过 |
| leader | school_leader | 通过 | 通过 | 通过 |
| office | office_admin | 通过 | 通过 | 通过 |
| finance | finance_staff | 通过 | 通过 | 通过 |
| security | security_staff | 通过 | 通过 | 通过 |
| keeper | seal_keeper | 通过 | 通过 | 通过 |

权限兜底：

| 场景 | 预期 | 实际 |
|---|---|---|
| admin 访问 `/api/admin/users` | 成功 | 成功，返回 18 个用户 |
| user 访问 `/api/admin/users` | 403 | 403 |
| 未登录访问 `/api/dashboard` | 401 | 401 |

## 5. 本次新增测试用例

新增文件：

- `frontend/src/views/Seals.spec.js`
- `frontend/src/views/Travels.spec.js`
- `frontend/src/views/Reports.spec.js`
- `frontend/src/views/Statistics.spec.js`
- `frontend/src/views/NotFound.spec.js`

### 5.1 用印管理

覆盖：

- 用印申请按钮
- 印章移交按钮
- 材料管理按钮
- 提交审批按钮
- 登记用印按钮
- 确认归还按钮
- 流程导览按钮
- `seal_keeper`、`office_admin`、`admin` 管理权限变量
- 外带原因、使用地点、监督人、预计归还时间
- 文件上传格式限制
- 显示已删除材料
- 删除原因必填

### 5.2 差旅审批

覆盖：

- 差旅申请按钮
- 目的地、出差事由、出发日期、返回日期
- 人员类别、出差类型、交通工具
- 提交差旅按钮
- approved 状态下的报销登记
- 实际报销金额、票据附件地址、超标准说明
- 提交财务复核按钮
- 流程导览入口

### 5.3 请示报告

覆盖：

- 提交请示报告按钮
- 标题、类型、密级、内容
- 字典下拉来源
- 创建报告接口调用
- approved 状态下的批复归档
- 批复意见输入
- 流程导览入口

### 5.4 统计报表

覆盖：

- 公文办理、待办公文、用印申请
- 会议申请、大型活动、差旅申请、请示报告
- 差旅预算总额
- 统计数据加载
- CSV 导出
- 业务状态字典标签
- 月度业务量

### 5.5 404 页面

覆盖：

- 页面不存在提示
- 返回首页按钮
- 跳转 `/dashboard`
- 不清空 sessionStorage
- 不触发 logout

## 6. 页面与角色覆盖摘要

### 6.1 页面覆盖

| 页面 | 自动化覆盖状态 |
|---|---|
| 登录页 | 已有前端测试 + API 登录烟测 |
| 工作台 | 已有前端测试 + 后端 DashboardScopeTest + API 烟测 |
| 公文管理 | 已有前端测试 + 后端闭环/权限测试 |
| 印章管理 | 本次新增页面测试 + 后端材料管理测试 |
| 会议管理 | 已有前端测试 + 后端会议参与人测试 |
| 邮件中心 | 已有前端测试 + 后端邮件集成测试 |
| 差旅审批 | 本次新增页面测试 + 后端工作流导览/策略测试 |
| 请示报告 | 本次新增页面测试 + 后端工作流导览测试 |
| 审批任务 | 已有前端测试 + 后端审批链测试 |
| 通知公告 | 已有前端测试 + 后端公告集成测试 |
| 统计报表 | 本次新增页面测试 + 前端构建验证 |
| 用户管理 | 已有前端测试 + API 管理权限烟测 |
| 字典管理 | 已有前端测试 + 后端字典中心测试 |
| 404 页面 | 本次新增页面测试 + 路由测试 |

### 6.2 角色覆盖

| 角色 | 覆盖内容 |
|---|---|
| admin | 登录、工作台、公告、用户管理接口、菜单权限测试 |
| office_user | 登录、工作台、公告、admin 越权 403、菜单权限测试 |
| dept_head | 登录、工作台、公告、菜单权限测试、审批相关后端测试 |
| school_leader | 登录、工作台、公告、菜单权限测试、审批相关后端测试 |
| office_admin | 登录、工作台、公告、菜单权限测试、公文/公告/请示相关后端测试 |
| finance_staff | 登录、工作台、公告、菜单权限测试、差旅相关后端测试 |
| security_staff | 登录、工作台、公告、菜单权限测试、大型活动审批相关后端测试 |
| seal_keeper | 登录、工作台、公告、菜单权限测试、用印登记相关前端测试 |

## 7. 缺陷与修复

本次未发现需要修改生产代码的 P0/P1 缺陷。

已完成的改进：

- 补充 5 个缺失页面的前端回归用例。
- 将前端测试覆盖从 23 个测试文件、95 个用例扩展到 28 个测试文件、106 个用例。

## 8. 未完全覆盖与风险

以下内容未在本次自动化中完全完成，需要后续专项环境或更长 E2E 测试窗口覆盖：

1. MySQL profile 启动后的真实重启持久化验证未执行。
2. 浏览器插件连接失败，错误为本地内核资源路径不存在；因此没有使用 Codex in-app Browser 完成可视化截图。
3. 临时 Playwright 包在当前 Windows/npm 环境中无法被 Node 脚本 import，因此没有完成 Playwright 浏览器级多账号菜单脚本。
4. 真实文件上传下载只由后端材料测试和前端源码断言覆盖，未在浏览器中选择真实文件。
5. 邮件外部 SMTP 未启用，外部投递只覆盖站内邮件和状态逻辑。
6. 性能、兼容性、移动端真实设备未做专项验证。

## 9. 回归通过判定

按本次可执行范围判定为通过：

- P0 登录、认证、权限、核心后端规则通过自动化验证。
- P1 主要页面入口、按钮、弹窗字段已由前端源码级测试覆盖。
- 后端全量测试通过。
- 前端全量测试通过。
- 前端构建通过。
- 多角色 API 烟测通过。

建议后续如果要达到交付级完全回归，再追加一次 MySQL profile、真实浏览器截图、真实文件上传下载和完整业务闭环 E2E。
